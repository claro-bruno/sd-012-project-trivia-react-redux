const INITIAL_STATE = {
  name: '',
  email: 'email@email.com',
  profilePicture: '',
};

const user = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default user;
